name: Workflow
on:
  push:
    branches:
      - main
jobs:
  BUILD:
    runs-on: ubuntu-latest
    steps:

      - name: Building
        run: |
          echo 'Aqui compilamos'
  TEST:
    runs-on: ubuntu-latest
    steps:

      - name: Testing
        run: |
          echo 'Testing'